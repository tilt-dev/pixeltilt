local_resource(
  'main',
  serve_cmd='go run main.go',
  deps=['main.go', 'index.html']
)

local_resource(
  'max-image-resolution-enhancer',
  serve_cmd='docker run -p 5000:5000 codait/max-image-resolution-enhancer'
)
